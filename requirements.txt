torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0
transformers==4.46.3
tokenizers==0.20.3
flask>=3.0.0
flask-cors>=4.0.0
Pillow>=10.0.0
addict>=2.4.0
matplotlib>=3.7.0
einops>=0.7.0
easydict>=1.10
safetensors>=0.4.0
accelerate>=0.25.0
hf-xet>=0.1.0

# Batch processing dependencies
pyyaml>=6.0.0           # YAML config file parsing
pynvml>=11.5.0          # GPU memory monitoring (NVIDIA Management Library)

# Medical entity extraction (NER)
spacy>=3.7.0            # Spanish NLP and NER
# After installing spacy, download Spanish model:
# python -m spacy download es_core_news_md

# PostgreSQL database with pgvector
psycopg2-binary>=2.9.0  # PostgreSQL adapter
# Alternative: psycopg2>=2.9.0 (requires PostgreSQL dev libraries)

# Note: transformers already included above for DeepSeek-OCR
# Used for embedding generation with multilingual-e5-large-instruct
